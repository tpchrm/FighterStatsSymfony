{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-flex h-100 justify-content-center align-items-center">
        <div class="alert alert-success text-center">
            <h1 class="display-5">Remplissage de la base de données effectué !</h1>
            <a class="btn btn-success rounded-pill" href="{{ path('main_menu') }}">Menu principal</a>
        </div>
    </div>
{% endblock %}

