{% extends 'base.html.twig' %}

{% block body %}
    <div class="row py-3">
        <div class="col-10">
            <h1 class="fw-bold text-center">Liste de tous les utilisateurs</h1>
        </div>
        <div class="d-flex align-items-center col-2">
            <a class="btn btn-secondary" href="{{ path('app_register') }}">Ajouter un utilisateur</a>
        </div>
    </div>
    <table class="table table-dark table-bordered table-striped table-hover text-center">
        <thead>
        <tr>
            <td>Identifiant</td>
            <td>Nom</td>
            <td>Prénom</td>
{#            <td>Détails</td>#}
        </tr>
        </thead>
        <tbody>
        {% for user in list_users %}
            <tr>
                <td>{{ user.username }}</td>
                <td>{{ user.lastname }}</td>
                <td>{{ user.firstname }}</td>
{#                <td>#}
{#                    <a class="btn btn-info" href="">Voir</a>#}
{#                    <a class="btn btn-warning" href="">Éditer</a>#}
{#                    <a class="btn btn-danger" href="">Supprimer</a>#}
{#                </td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
