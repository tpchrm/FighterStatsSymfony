{% extends 'base.html.twig' %}

{% block navlinks %}
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" href="{{ path('main_menu') }}">Accueil</a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" href="{{ path('fighter_men') }}">Combattants</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('fight_list') }}">Combats</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('help_page') }}">Aide & Documentation</a>
        </li>
    </ul>
{% endblock %}

{% block returnlink %}
    <button class="btn btn-outline-danger rounded-pill" onclick="history.back();">Retour</button>
{% endblock %}

{% block body %}
    <h1 class="text-center">Formulaire d'ajout d'un Combattant</h1>

            {{ form_start(fighter_men_add, {
                'attr':{'class':'row g-3'}
            }) }}
            <div class="col-md-6">
                {{ form_row(fighter_men_add.lastname, {
                        'label':'Nom'
                }) }}
            </div>
            <div class="col-md-6">
                {{ form_row(fighter_men_add.firstname, {
                        'label':'Prénom'
                }) }}
            </div>
            <div class="col-md-6">
                {{ form_row(fighter_men_add.height, {
                        'label':'Poids (en livres)'
                }) }}
            </div>
            <div class="col-md-6">
                {{ form_row(fighter_men_add.weight, {
                        'label':'Taille (en pouces)'
                }) }}
            </div>
            <div class="col-md-6">
                {{ form_row(fighter_men_add.origin, {
                        'label':'Pays'
                }) }}
            </div>
            <div class="col-md-6">
                {{ form_row(fighter_men_add.division, {
                        'label':'Division'
                }) }}
            </div>
        <div class="col-md-12">
                {{ form_row(fighter_men_add.enregistrer, {
                        'attr':{'class':'btn-danger w-100'}
                }) }}
        </div>
            {{ form_end(fighter_men_add) }}

{% endblock %}
